* learned about git ,vcs control,installation
* got toknow and learn about U-untracked M-Modified A-added/tracking 
* to track "git add <filename>
* to save final changes "git commit-m "msg""
* to see changes git diff
* can see what are changes made in graph and to see commits git log --oneline or git log
* learned about collaborating andpushing existing code to git hub
* pushing a folder with files for created repo
   -git remote add origin https://github.com/username/reponame 
   -git branch -M main
   -git push -u origin main-its for only the beginning of repo
* use git push after commit no need 3 extra steps
* got to know about 4 stage
  -working dir stage
  -staging area stage
  -commit area stage
  -github stage